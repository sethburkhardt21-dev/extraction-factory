# Hermes Source-First Benchmark Pack v1

This harness is implemented, but semantic gold is intentionally **not authored from prior extraction output**.

The Machines pilot source units are copied into `SOURCE_UNITS/`. A future gold-construction pass must independently read the source, create reference assertions, freeze them, and only then reveal SOL/Kimi/Gemini/Hermes candidates for challenge.

Current state: `READY_TO_CONSTRUCT_SOURCE_FIRST_GOLD / NOT_BENCHMARKED`.

# Minimum Context Policy v0.1

Exclude by default: prior PASS claims, candidate counts/density expectations, unrelated history/queues, model reputation, prior worker reasoning, whole forensic ZIPs, downstream promotion state. Include boundary pages, literal inventories, candidate families, competing bytes/proposals only when the role requires them. Manifest every exception.

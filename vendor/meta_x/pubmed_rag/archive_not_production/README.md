# Disabled legacy modules

These files are retained as inert `.txt` lineage evidence only. They are not importable production code because they contained synthetic/placeholder data behavior superseded by repaired modules.

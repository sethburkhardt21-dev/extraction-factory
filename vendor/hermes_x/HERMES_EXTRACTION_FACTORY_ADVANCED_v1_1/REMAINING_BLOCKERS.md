# HERMES Advanced v1.1 — Genuine Remaining Blockers

## Blocks empirical semantic extraction claims

1. **No real semantic model/provider is available in this execution environment.**
   - No `hermes`, `ollama`, `llama-cli`, or equivalent model executable was present.
   - The generic JSON command provider is implemented and tested.

2. **No semantic model role is certified.**
   - Model certification registry is intentionally empty.
   - Primary and blind roles therefore remain `UNBENCHMARKED`.

3. **Source-first semantic gold is not constructed.**
   - Benchmark harness/source units are prepared.
   - Prior SOL/Kimi/Gemini output has not been used as initial gold.

4. **Independent semantic cold-audit provider is not wired/certified.**

5. **True image/figure semantic interpretation is not wired/certified.**
   - Caption/text-layer and visual-risk routing are implemented.
   - Image understanding is not falsely claimed.

6. **Ambiguous W3 semantic binding is intentionally queued.**
   - numeric target binding
   - qualifier scope
   - relationship direction/causality
   - table row/column binding
   - cross-page semantic continuation

## Blocks live production operations, not offline mechanics

7. Actual Hermes/Claude/provider wrapper command needs to be configured.
8. Live SAFE_4 → BALANCED_8 → HIGH_12 semantic concurrency/soak must be measured.
9. Provider outage/failover and overnight soak must run against real providers.
10. Actual current 09D database was not supplied to this Hermes-only execution; only read-only bridge behavior is offline-tested.
11. Buzz runtime was not available, so native/sidecar disconnect behavior remains unverified here.

## Does not remain a blocker

The following are now actual code rather than architecture-only gaps:

- candidate schema/invariants
- primary/blind transport
- blindness allowlist
- numeric/qualifier/relationship literal inventories
- deterministic union
- evidence families
- deterministic specialist controls
- precision integrity review
- risk routing
- cold-audit sampling mechanics
- legal durable work state machine
- concurrent worker dispatch
- staged/torn-commit protection
- CAS/stale worker rejection
- event/state reconciliation
- build/current manifest split
- runtime lock
- generalized PDF ingestion
- provider-neutral external command bridge
- fail-closed readiness
- generalized offline handoff package
- canonical one-entrypoint CLI
